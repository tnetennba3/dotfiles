# Dotfiles

Development environment configuration.

## Setup

```bash
git clone https://github.com/tnetennba3/dotfiles.git
cd ~/dotfiles
chmod +x install.sh
./install.sh
```

## What's included

- **Git** — global gitconfig with aliases, default branch, editor, and pull strategy
